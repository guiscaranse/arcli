dist: bionic
language: python
python:
- '3.6'
install:
- pip install poetry nuitka
- poetry config settings.virtualenvs.create false
- poetry install
script:
- mv arcli/__main__.py arcli.py
- python -m nuitka --follow-imports --recurse-all --show-progress arcli.py
